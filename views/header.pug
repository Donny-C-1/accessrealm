header 
    .logo
        a(href="/")
            img(src="/images/logo.png" alt="Access Realm Logo" width="100%" height="auto")
    nav 
        a(href="/messages") Messages
        if (user)
            a(href="/messages/create") Add Message
            a(href="/membership") Become a Member
        if (user)
            a(href="/auth/logout") Logout
        else 
            a(href="/auth/login") Login
br
br
br